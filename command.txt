git config --global user.name "sahla"

git config --global user.email "sahlats03@gmail.com"

git config --list

git add -A (to add untracked file into staging area)

git init (to initialize)

git commit -m ("to add initial message")

git status ( to list untracked files)

git log 

git checkout

git checkout master

git remote add remote_name remote_path ()


git push remote_name branch_name